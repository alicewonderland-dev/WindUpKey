namespace WindUpKey;

// Copy this file to RelaySecrets.cs (gitignored — never commit the real value)
// and set RelayToken to the same value as Relay:Token in every host relay's
// appsettings.Production.json. RelayDefaults.RelayToken reads from here.
internal static class RelaySecrets
{
    public const string RelayToken = "CHANGE_ME_TO_A_LONG_RANDOM_SECRET";
}
